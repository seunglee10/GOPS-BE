# Deprecated: Team Merge Guide

This guide is retained only so older references do not break.
Do not use previous revisions of this file for chart work.

Use the current rebuild contract:

```text
../docs/CHART_DATA_REBUILD_PLAN.md
```

When implementing the rewrite, preserve only durable repository rules from
`AGENTS.md`, `docs/STRUCTURE_GUIDE.md`, and the current code. For chart data,
the new plan wins over this folder's old history.
