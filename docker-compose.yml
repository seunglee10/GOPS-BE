# 역할: 로컬 한 대의 Docker에서 운영 아키텍처의 각 서버 역할을 컨테이너로 흉내 냅니다.
# 사용: AWS/EKS 배포 전 연결 계약과 데이터 흐름만 검증합니다.
# 주의: 운영에서는 Kafka/MSK, Flink, Redis, S3가 분리된 서버/관리형 서비스입니다.
# 구역: Docker network를 app/streaming/data로 나눠 AWS subnet 구조를 가볍게 흉내 냅니다.
services:
  kafka:
    image: apache/kafka:latest
    container_name: alfaka-kafka
    ports:
      - "9092:9092"
    environment:
      CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"
      KAFKA_NODE_ID: "1"
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@localhost:9093"
      KAFKA_LISTENERS: "INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092,CONTROLLER://localhost:9093"
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka:29092,EXTERNAL://localhost:9092"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      KAFKA_INTER_BROKER_LISTENER_NAME: "INTERNAL"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: "1"
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: "0"
      KAFKA_NUM_PARTITIONS: "3"
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_LOG_DIRS: "/tmp/kraft-combined-logs"
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:29092 --list >/dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 12
    networks:
      - streaming_net

  kafka-init:
    image: apache/kafka:latest
    container_name: alfaka-kafka-init
    depends_on:
      kafka:
        condition: service_healthy
    entrypoint:
      - /bin/bash
      - -ec
      - |
        for topic in market.raw.bars market.raw.updated-bars market.raw.trades market.raw.daily-bars market.raw.statuses market.raw.quotes market.raw.corrections market.raw.cancel-errors market.ticks.v1 market.candles.live.1m.v1 market.candles.closed.v1 market.status.v1 market.volume-profile-bins.1m.v1 market.news.alpaca.v1 orders.commands.v1 broker.submit-results.v1 broker.order-events.v1 orders.dlq.v1 agents.market-events.v1 agents.analysis-requests.v1 agents.analysis-results.v1 agents.notification-decisions.v1 agents.dlq.v1; do
          /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka:29092 --create --if-not-exists --topic "$${topic}" --partitions 3 --replication-factor 1
        done
        /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka:29092 --list
    restart: "no"
    networks:
      - streaming_net

  redis:
    image: redis:7-alpine
    container_name: alfaka-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data_net

  postgres:
    image: postgres:16-alpine
    container_name: alfaka-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: "gops"
      POSTGRES_USER: "gops"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-gops_dev_password}"
    volumes:
      - postgres_order_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gops -d gops >/dev/null 2>&1"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data_net

  minio:
    profiles: ["local-s3"]
    image: minio/minio:latest
    container_name: alfaka-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: "minioadmin"
      MINIO_ROOT_PASSWORD: "minioadmin"
    command: ["server", "/data", "--console-address", ":9001"]
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/minio/health/live >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data_net

  minio-init:
    profiles: ["local-s3"]
    image: minio/mc:latest
    container_name: alfaka-minio-init
    environment:
      S3_BUCKET: "${S3_BUCKET:-gops-market-data-<aws-account-id>-ap-northeast-2-an}"
    depends_on:
      minio:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - |
        mc alias set local http://minio:9000 minioadmin minioadmin
        mc mb -p "local/$${S3_BUCKET}" || true
        mc ls local
    restart: "no"
    networks:
      - data_net

  clickhouse:
    image: clickhouse/clickhouse-server:24.12-alpine
    container_name: alfaka-clickhouse
    ports:
      - "8123:8123"
      - "9002:9000"
    environment:
      CLICKHOUSE_DB: "market_data"
      CLICKHOUSE_USER: "alfaka"
      CLICKHOUSE_PASSWORD: "alfaka"
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./infra/clickhouse/initdb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "clickhouse-client --user alfaka --password alfaka --query 'SELECT 1' >/dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 20
    networks:
      - data_net

  graphdb:
    profiles: ["graphdb"]
    image: ontotext/graphdb:11.4.0
    container_name: alfaka-graphdb
    ports:
      - "7200:7200"
    environment:
      GDB_HEAP_SIZE: "${GRAPHDB_HEAP_SIZE:-4g}"
    volumes:
      - graphdb_data:/opt/graphdb/home
    restart: unless-stopped
    networks:
      - data_net

  alpaca-ingestor:
    profiles: ["alpaca"]
    image: gops-market-ingestor
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-ingestor
    container_name: alfaka-alpaca-ingestor
    env_file: .env
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      REDIS_URL: "redis://redis:6379/0"
      REDIS_KEY_PREFIX: ""
      ALPACA_CHANNELS: "bars,updatedBars,dailyBars,statuses"
      ALPACA_ACTIVE_CHANNELS: "trades"
      ALPACA_ACTIVE_TICK_SUBSCRIPTION: "true"
    command: ["python", "-u", "systems/market-data/pods/market-ingestor/market_stream.py"]
    restart: unless-stopped
    networks:
      - app_net
      - data_net
      - streaming_net

  alpaca-news-ingestor:
    profiles: ["alpaca"]
    image: gops-market-ingestor
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-ingestor
    container_name: alfaka-alpaca-news-ingestor
    env_file: .env
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      KAFKA_NEWS_TOPIC: "market.news.alpaca.v1"
      ALPACA_NEWS_POLL_SECONDS: "${ALPACA_NEWS_POLL_SECONDS:-300}"
      ALPACA_NEWS_LIMIT: "${ALPACA_NEWS_LIMIT:-50}"
      ALPACA_NEWS_INCLUDE_CONTENT: "${ALPACA_NEWS_INCLUDE_CONTENT:-false}"
    command: ["python", "-u", "systems/market-data/pods/news-ingestor/main.py"]
    restart: unless-stopped
    networks:
      - app_net
      - streaming_net

  local-stream-processor:
    image: gops-market-processor
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-processor
    container_name: alfaka-local-stream-processor
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      KAFKA_RAW_TOPIC_PREFIX: "market.raw"
      KAFKA_TICKS_TOPIC: "market.ticks.v1"
      KAFKA_LIVE_CANDLE_TOPIC: "market.candles.live.1m.v1"
      KAFKA_CLOSED_CANDLE_TOPIC: "market.candles.closed.v1"
      KAFKA_STATUS_TOPIC: "market.status.v1"
      KAFKA_VOLUME_PROFILE_BINS_TOPIC: "market.volume-profile-bins.1m.v1"
      KAFKA_FLINK_GROUP_ID: "alfaka-local-stream-processor"
      VOLUME_PROFILE_PRICE_BIN_SIZE: "0.05"
      PROCESSOR_LOG_EVERY_N: "500"
      REDIS_URL: "redis://redis:6379/0"
      REDIS_KEY_PREFIX: ""
    command: ["python", "-u", "systems/market-data/pods/market-processor/local_main.py"]
    restart: unless-stopped
    networks:
      - app_net
      - streaming_net
      - data_net

  s3-sink:
    image: gops-market-storage
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-storage
    container_name: alfaka-s3-sink
    env_file: .env
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      KAFKA_S3_GROUP_ID: "alfaka-processed-s3-sink"
      KAFKA_PROCESSED_TOPICS: "market.ticks.v1,market.candles.live.1m.v1,market.candles.closed.v1,market.status.v1,market.volume-profile-bins.1m.v1"
      AWS_REGION: "${AWS_REGION:-ap-northeast-2}"
      AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION:-ap-northeast-2}"
      AWS_EC2_METADATA_DISABLED: "${AWS_EC2_METADATA_DISABLED:-true}"
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID:-}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY:-}"
      AWS_SESSION_TOKEN: "${AWS_SESSION_TOKEN:-}"
      S3_ENDPOINT_URL: "${DOCKER_S3_ENDPOINT_URL:-}"
      S3_BUCKET: "${S3_BUCKET:-gops-market-data-<aws-account-id>-ap-northeast-2-an}"
      S3_FINAL_PREFIX: "${S3_FINAL_PREFIX:-market-data/final}"
      S3_LIVE_PREFIX: "${S3_LIVE_PREFIX:-market-data/live}"
      S3_PROCESSED_FORMAT: "${S3_PROCESSED_FORMAT:-parquet}"
      S3_FLUSH_COUNT: "${S3_FLUSH_COUNT:-1000}"
    command: ["python", "-u", "systems/market-data/pods/s3-sink/processed_sink.py"]
    restart: unless-stopped
    networks:
      - app_net
      - streaming_net
      - data_net

  clickhouse-loader:
    image: gops-market-storage
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-storage
    container_name: alfaka-clickhouse-loader
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      clickhouse:
        condition: service_healthy
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      KAFKA_CLICKHOUSE_GROUP_ID: "alfaka-clickhouse-loader"
      KAFKA_CLICKHOUSE_TOPICS: "market.candles.closed.v1,market.status.v1,market.volume-profile-bins.1m.v1,market.news.alpaca.v1"
      KAFKA_NEWS_TOPIC: "market.news.alpaca.v1"
      CLICKHOUSE_LOAD_TRADES: "false"
      CLICKHOUSE_HTTP_URL: "http://clickhouse:8123"
      CLICKHOUSE_DATABASE: "market_data"
      CLICKHOUSE_USER: "alfaka"
      CLICKHOUSE_PASSWORD: "alfaka"
    command: ["python", "-u", "systems/market-data/pods/clickhouse-loader/processed_loader.py"]
    restart: unless-stopped
    networks:
      - app_net
      - streaming_net
      - data_net

  symbol-registry-sync:
    profiles: ["registry", "alpaca"]
    image: gops-market-processor
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-processor
    container_name: alfaka-symbol-registry-sync
    env_file: .env
    depends_on:
      clickhouse:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      CLICKHOUSE_HTTP_URL: "http://clickhouse:8123"
      CLICKHOUSE_DATABASE: "market_data"
      CLICKHOUSE_USER: "alfaka"
      CLICKHOUSE_PASSWORD: "alfaka"
      REDIS_URL: "redis://redis:6379/0"
      REDIS_KEY_PREFIX: ""
      SYMBOL_REGISTRY_ASSET_CLASS: "us_equity"
      SYMBOL_REGISTRY_STATUS: "active"
      SYMBOL_REGISTRY_REDIS_CACHE: "true"
    command: ["python", "-u", "systems/market-data/jobs/symbol-registry-sync/main.py"]
    restart: "no"
    networks:
      - app_net
      - data_net

  coverage-repair:
    profiles: ["repair"]
    image: gops-market-processor
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-market-processor
    container_name: alfaka-coverage-repair
    env_file: .env
    depends_on:
      gops-backend:
        condition: service_started
      backfill-worker:
        condition: service_started
    environment:
      GOPS_API_BASE_URL: "http://gops-backend:8000"
      COVERAGE_REPAIR_SYMBOLS: "${COVERAGE_REPAIR_SYMBOLS:-NVDA,AMD,AVGO,TSM,ASML,AMAT,MU}"
      COVERAGE_REPAIR_INTERVALS: "${COVERAGE_REPAIR_INTERVALS:-1m,5m,10m,1D,1W,1M}"
      COVERAGE_REPAIR_DRY_RUN: "${COVERAGE_REPAIR_DRY_RUN:-true}"
      COVERAGE_REPAIR_FORCE: "${COVERAGE_REPAIR_FORCE:-false}"
    command: ["python", "-u", "systems/market-data/jobs/coverage-repair/main.py"]
    restart: "no"
    networks:
      - app_net
      - data_net

  agent-orchestrator:
    image: gops-agent-orchestrator
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-agent-orchestrator
    container_name: alfaka-agent-orchestrator
    env_file: .env
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      REDIS_URL: "redis://redis:6379/0"
      AGENT_MARKET_EVENTS_TOPIC: "agents.market-events.v1"
      AGENT_ANALYSIS_REQUESTS_TOPIC: "agents.analysis-requests.v1"
      AGENT_ANALYSIS_RESULTS_TOPIC: "agents.analysis-results.v1"
      AGENT_NOTIFICATION_DECISIONS_TOPIC: "agents.notification-decisions.v1"
      AGENT_DLQ_TOPIC: "agents.dlq.v1"
      AGENT_PUBLISH_TO_KAFKA: "true"
      CLICKHOUSE_HTTP_URL: "http://clickhouse:8123"
      CLICKHOUSE_DATABASE: "market_data"
      CLICKHOUSE_USER: "alfaka"
      CLICKHOUSE_PASSWORD: "alfaka"
      GRAPHDB_SPARQL_URL: "${GRAPHDB_SPARQL_URL:-http://graphdb:7200/repositories/nasdaq-fibo}"
      GRAPHDB_REPOSITORY: "${GRAPHDB_REPOSITORY:-nasdaq-fibo}"
      AGENT_ONTOLOGY_LIMIT: "${AGENT_ONTOLOGY_LIMIT:-20}"
    ports:
      - "8100:8100"
    restart: unless-stopped
    networks:
      - app_net
      - data_net
      - streaming_net

  agent-event-detector:
    image: gops-agent-orchestrator
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-agent-orchestrator
    container_name: alfaka-agent-event-detector
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      AGENT_EVENT_INPUT_TOPICS: "market.ticks.v1,market.candles.live.1m.v1,market.candles.closed.v1"
      AGENT_MARKET_EVENTS_TOPIC: "agents.market-events.v1"
    command: ["python", "-u", "/app/systems/agent-orchestration/pods/event-detector/main.py"]
    restart: unless-stopped
    networks:
      - streaming_net

  agent-notification-publisher:
    image: gops-agent-orchestrator
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-agent-orchestrator
    container_name: alfaka-agent-notification-publisher
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      REDIS_URL: "redis://redis:6379/0"
      AGENT_NOTIFICATION_DECISIONS_TOPIC: "agents.notification-decisions.v1"
    command: ["python", "-u", "/app/systems/agent-orchestration/pods/notification-publisher/main.py"]
    restart: unless-stopped
    networks:
      - data_net
      - streaming_net

  gops-backend:
    image: gops-api-server
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-backend
    container_name: alfaka-gops-backend
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      postgres:
        condition: service_healthy
      order-migrations:
        condition: service_completed_successfully
      agent-orchestrator:
        condition: service_started
    environment:
      REDIS_URL: "redis://redis:6379/0"
      AUTH_ENABLED: "${AUTH_ENABLED:-false}"
      AUTH_PUBLIC_BASE_URL: "${AUTH_PUBLIC_BASE_URL:-http://localhost:5173}"
      AUTH_SESSION_SECRET: "${AUTH_SESSION_SECRET:-}"
      AUTH_SESSION_TTL_SECONDS: "${AUTH_SESSION_TTL_SECONDS:-28800}"
      AUTH_OAUTH_STATE_TTL_SECONDS: "${AUTH_OAUTH_STATE_TTL_SECONDS:-300}"
      AUTH_REDIS_URL: "${AUTH_REDIS_URL:-redis://redis:6379/0}"
      AUTH_REDIS_KEY_PREFIX: "${AUTH_REDIS_KEY_PREFIX:-gops:auth}"
      AUTH_COOKIE_SECURE: "${AUTH_COOKIE_SECURE:-false}"
      AUTH_COOKIE_SAMESITE: "${AUTH_COOKIE_SAMESITE:-lax}"
      GOOGLE_OAUTH_CLIENT_ID: "${GOOGLE_OAUTH_CLIENT_ID:-}"
      GOOGLE_OAUTH_CLIENT_SECRET: "${GOOGLE_OAUTH_CLIENT_SECRET:-}"
      GOOGLE_OAUTH_SECRET_NAME: "${GOOGLE_OAUTH_SECRET_NAME:-}"
      DATABASE_URL: "postgresql://gops:${POSTGRES_PASSWORD:-gops_dev_password}@postgres:5432/gops"
      AGENT_ORCHESTRATOR_URL: "http://agent-orchestrator:8100"
      ORDER_REPOSITORY: "postgres"
      KIS_ENV: "${KIS_ENV:-demo}"
      KAFKA_ACCOUNT_ALIAS: "${KAFKA_ACCOUNT_ALIAS:-demo-account}"
      IDEMPOTENCY_HASH_SECRET: "${IDEMPOTENCY_HASH_SECRET:-local-dev-idempotency-secret}"
      CLICKHOUSE_HTTP_URL: "http://clickhouse:8123"
      CLICKHOUSE_DATABASE: "market_data"
      CLICKHOUSE_USER: "alfaka"
      CLICKHOUSE_PASSWORD: "alfaka"
      ALFAKA_PACKAGES_PATH: "/app/systems/market-data/shared"
      REDIS_KEY_PREFIX: ""
      BACKFILL_EXECUTION_MODE: "queue"
      BACKFILL_STATUS_TTL_SECONDS: "86400"
      BACKFILL_DEFAULT_LOOKBACK_HOURS: "8760"
      AWS_REGION: "${AWS_REGION:-ap-northeast-2}"
      AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION:-ap-northeast-2}"
      AWS_EC2_METADATA_DISABLED: "${AWS_EC2_METADATA_DISABLED:-true}"
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID:-}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY:-}"
      AWS_SESSION_TOKEN: "${AWS_SESSION_TOKEN:-}"
      ALPACA_SECRET_NAME: "${ALPACA_SECRET_NAME:-dev/alpaca}"
      S3_ENDPOINT_URL: "${DOCKER_S3_ENDPOINT_URL:-}"
      S3_BUCKET: "${S3_BUCKET:-gops-market-data-<aws-account-id>-ap-northeast-2-an}"
      S3_RAW_PREFIX: "${S3_RAW_PREFIX:-market-data/raw/alpaca}"
      S3_FINAL_PREFIX: "${S3_FINAL_PREFIX:-market-data/final}"
      S3_PROCESSED_FORMAT: "${S3_PROCESSED_FORMAT:-jsonl}"
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - app_net
      - data_net

  order-migrations:
    image: gops-order-worker
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-order-worker
    container_name: alfaka-order-migrations
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgresql://gops:${POSTGRES_PASSWORD:-gops_dev_password}@postgres:5432/gops"
    command: ["python", "-u", "systems/order/jobs/migrations/main.py"]
    restart: "no"
    networks:
      - data_net

  order-outbox-publisher:
    image: gops-order-worker
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-order-worker
    container_name: alfaka-order-outbox-publisher
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      order-migrations:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: "postgresql://gops:${POSTGRES_PASSWORD:-gops_dev_password}@postgres:5432/gops"
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      KAFKA_ACCOUNT_ALIAS: "${KAFKA_ACCOUNT_ALIAS:-demo-account}"
      IDEMPOTENCY_HASH_SECRET: "${IDEMPOTENCY_HASH_SECRET:-local-dev-idempotency-secret}"
    command: ["python", "-u", "systems/order/pods/order-outbox/main.py"]
    restart: unless-stopped
    networks:
      - data_net
      - streaming_net

  kis-broker-adapter:
    image: gops-kis-adapter
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-kis-adapter
    container_name: alfaka-kis-broker-adapter
    env_file: .env
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      order-migrations:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: "postgresql://gops:${POSTGRES_PASSWORD:-gops_dev_password}@postgres:5432/gops"
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
      KIS_ENV: "${KIS_ENV:-demo}"
      KIS_SECRET_NAME: "${KIS_SECRET_NAME:-dev/kis}"
      KIS_DEMO_APP_KEY: "${KIS_DEMO_APP_KEY:-}"
      KIS_DEMO_APP_SECRET: "${KIS_DEMO_APP_SECRET:-}"
      KIS_DEMO_ACCOUNT_NO: "${KIS_DEMO_ACCOUNT_NO:-}"
      KIS_ACCOUNT_PRODUCT_CODE: "${KIS_ACCOUNT_PRODUCT_CODE:-01}"
      KIS_TOKEN_CACHE_PATH: "/kis-cache/demo-token.json"
      KIS_TIMEOUT_SECONDS: "${KIS_TIMEOUT_SECONDS:-5}"
    command: ["python", "-u", "systems/order/pods/kis-adapter/main.py"]
    restart: unless-stopped
    volumes:
      - kis_token_cache:/kis-cache
    networks:
      - data_net
      - streaming_net

  order-reconciler:
    profiles: ["reconciliation"]
    image: gops-order-worker
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-order-worker
    container_name: alfaka-order-reconciler
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      order-migrations:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: "postgresql://gops:${POSTGRES_PASSWORD:-gops_dev_password}@postgres:5432/gops"
      KIS_ENV: "${KIS_ENV:-demo}"
      KIS_SECRET_NAME: "${KIS_SECRET_NAME:-dev/kis}"
      KIS_TOKEN_CACHE_PATH: "/kis-cache/demo-token.json"
      KIS_TIMEOUT_SECONDS: "${KIS_TIMEOUT_SECONDS:-5}"
    command: ["python", "-u", "systems/order/jobs/reconciler/main.py"]
    restart: "no"
    volumes:
      - kis_token_cache:/kis-cache
    networks:
      - data_net

  backfill-worker:
    image: gops-backfill-worker
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-backfill-worker
    container_name: alfaka-backfill-worker
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    environment:
      REDIS_URL: "redis://redis:6379/0"
      CLICKHOUSE_HTTP_URL: "http://clickhouse:8123"
      CLICKHOUSE_DATABASE: "market_data"
      CLICKHOUSE_USER: "alfaka"
      CLICKHOUSE_PASSWORD: "alfaka"
      BACKFILL_EXECUTION_MODE: "queue"
      BACKFILL_WORKER_POLL_SECONDS: "5"
      BACKFILL_DEFAULT_LOOKBACK_HOURS: "8760"
      AWS_REGION: "${AWS_REGION:-ap-northeast-2}"
      AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION:-ap-northeast-2}"
      AWS_EC2_METADATA_DISABLED: "${AWS_EC2_METADATA_DISABLED:-true}"
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID:-}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY:-}"
      AWS_SESSION_TOKEN: "${AWS_SESSION_TOKEN:-}"
      ALPACA_SECRET_NAME: "${ALPACA_SECRET_NAME:-dev/alpaca}"
      S3_ENDPOINT_URL: "${DOCKER_S3_ENDPOINT_URL:-}"
      S3_BUCKET: "${S3_BUCKET:-gops-market-data-<aws-account-id>-ap-northeast-2-an}"
      S3_RAW_PREFIX: "${S3_RAW_PREFIX:-market-data/raw/alpaca}"
      S3_FINAL_PREFIX: "${S3_FINAL_PREFIX:-market-data/final}"
      S3_PROCESSED_FORMAT: "${S3_PROCESSED_FORMAT:-jsonl}"
    command: ["python", "-u", "systems/market-data/pods/backfill-worker/main.py"]
    restart: unless-stopped
    networks:
      - app_net
      - data_net

  gops-frontend:
    image: gops-frontend
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.gops-frontend
    container_name: alfaka-gops-frontend
    depends_on:
      - gops-backend
    ports:
      - "5173:5173"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:5173/healthz >/dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 12
    restart: unless-stopped
    networks:
      - app_net

volumes:
  redis_data:
  minio_data:
  clickhouse_data:
  graphdb_data:
    name: nasdaq_fibo_graphdb_data
    external: true
  postgres_order_data:
  kis_token_cache:

networks:
  app_net:
    name: alfaka-app-net
    driver: bridge
  streaming_net:
    name: alfaka-streaming-net
    driver: bridge
  data_net:
    name: alfaka-data-net
    driver: bridge
