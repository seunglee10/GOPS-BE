# Postgres Platform Contract

Current local stage:

```text
docker-compose postgres
systems/order/jobs/migrations
```

AWS/EKS can later point database env values at RDS.

Never commit real passwords or connection strings.
