# 역할: External Secrets Operator를 EKS dev 클러스터에 설치할 때 쓰는 Helm values입니다.
# 이유: webhook/cert-controller에는 CloudWatch/OpenTelemetry 자동 주입을 끄지 않으면
#       admission webhook 응답 지연으로 kubectl apply dry-run이 실패할 수 있습니다.
installCRDs: true

_disableAutoInstrumentation: &disableAutoInstrumentation
  cloudwatch.aws.amazon.com/auto-annotate-dotnet: "false"
  cloudwatch.aws.amazon.com/auto-annotate-java: "false"
  cloudwatch.aws.amazon.com/auto-annotate-nodejs: "false"
  cloudwatch.aws.amazon.com/auto-annotate-python: "false"
  instrumentation.opentelemetry.io/inject-dotnet: "false"
  instrumentation.opentelemetry.io/inject-java: "false"
  instrumentation.opentelemetry.io/inject-nodejs: "false"
  instrumentation.opentelemetry.io/inject-python: "false"

global:
  podAnnotations:
    <<: *disableAutoInstrumentation

podAnnotations:
  <<: *disableAutoInstrumentation

webhook:
  podAnnotations:
    <<: *disableAutoInstrumentation

certController:
  podAnnotations:
    <<: *disableAutoInstrumentation
