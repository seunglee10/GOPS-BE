# 역할: gops-backend가 agent-orchestrator pod로 HTTP 요청을 보낼 수 있게 합니다.
apiVersion: v1
kind: Service
metadata:
  name: agent-orchestrator
  namespace: alfaka-market-data
spec:
  selector:
    app: agent-orchestrator
  ports:
    - name: http
      port: 8100
      targetPort: 8100
