apiVersion: apps/v1
kind: Deployment
metadata:
  name: paper-replay-matcher
  namespace: alfaka-market-data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paper-replay-matcher
  template:
    metadata:
      labels:
        app: paper-replay-matcher
    spec:
      serviceAccountName: alfaka-market-data-sa
      containers:
        - name: paper-replay-matcher
          image: YOUR_ECR_REPOSITORY/gops-order-worker:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-u", "systems/order/pods/paper-replay-matcher/main.py"]
          envFrom:
            - configMapRef:
                name: alfaka-market-data-config
            - secretRef:
                name: alfaka-order-db-secret
                optional: true
          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              cpu: 500m
              memory: 256Mi
          startupProbe:
            exec:
              command: ["python", "-m", "kis_trader.runtime_heartbeat", "--max-age-seconds", "120"]
            periodSeconds: 5
            failureThreshold: 24
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command: ["python", "-m", "kis_trader.runtime_heartbeat", "--max-age-seconds", "30"]
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command: ["python", "-m", "kis_trader.runtime_heartbeat", "--max-age-seconds", "60"]
            periodSeconds: 20
            failureThreshold: 3
            timeoutSeconds: 5
