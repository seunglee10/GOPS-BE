# 역할: agent-orchestrator pod가 GraphDB SPARQL endpoint로 접근하게 합니다.
apiVersion: v1
kind: Service
metadata:
  name: graphdb
  namespace: alfaka-market-data
spec:
  selector:
    app: graphdb
  ports:
    - name: http
      port: 7200
      targetPort: 7200
