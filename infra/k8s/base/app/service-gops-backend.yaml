# 역할: EKS 내부에서 frontend/ingress가 GOPS backend로 접근하는 ClusterIP Service입니다.
apiVersion: v1
kind: Service
metadata:
  name: gops-backend
  namespace: alfaka-market-data
spec:
  type: ClusterIP
  selector:
    app: gops-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
