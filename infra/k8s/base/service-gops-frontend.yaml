# 역할: EKS 내부 또는 ingress가 GOPS frontend로 접근하는 Service입니다.
apiVersion: v1
kind: Service
metadata:
  name: gops-frontend
  namespace: alfaka-market-data
spec:
  type: ClusterIP
  selector:
    app: gops-frontend
  ports:
    - name: http
      port: 5173
      targetPort: 5173
