# GOPS Codex 운영 규칙

이 프로젝트에서 Codex로 작업할 때는 먼저 `docs/process/codex-workflow.md`를 확인한다.

- `GOPS Navigator`는 메인 협업 허브다. 최종 판단은 항상 Navigator에서 사용자와 함께 한다.
- `GOPS Compass`는 방향성 검토만 수행한다. 구현하거나 파일을 수정하지 않는다.
- `GOPS Forge`는 Navigator가 정리한 구현 요청만 수행한다. 요청이 불명확하면 구현하지 않고 질문한다.
- `4869`와 `8520`은 Navigator에서만 해석한다.
- `7410`은 Compass 또는 Forge에서만 해석하는 보고 트리거다.
- Compass 또는 Forge에서 `7410`을 받으면 새 작업을 시작하지 말고 최근 작업을 `Navigator 전달용 보고서`로 정리한다.
- thread management 도구가 있으면 `GOPS Navigator` thread `019effe8-d2d8-79c3-9e6c-98a5beedeafb`로 보고서를 직접 보낸다.
- 직접 보낼 수 없으면 현재 채팅방에 `Navigator 전달용 보고서`를 남기고, 자동 전달 실패 또는 불가를 명시한다.
- 어떤 경우에도 `7410` 요청을 조용히 종료하지 않는다.
